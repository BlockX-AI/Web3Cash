{
  "name": "web3cash",
  "version": "0.1.0",
  "private": true,
  "description": "Web3Cash — verified on-chain cashback platform. Monorepo.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm run build -ws --if-present",
    "dev:web": "pnpm run dev -w @web3cash/web",
    "dev:api": "pnpm run dev -w @web3cash/api",
    "dev:admin": "pnpm run dev -w @web3cash/admin",
    "dev:worker": "pnpm run dev -w @web3cash/worker",
    "dev": "pnpm run dev -ws --if-present",
    "lint": "pnpm run lint -ws --if-present",
    "typecheck": "pnpm run typecheck -ws --if-present",
    "test": "pnpm run test -ws --if-present",
    "test:api": "tsx scripts/test-api.ts",
    "test:all": "bash scripts/test-all.sh",
    "db:generate": "pnpm run generate -w @web3cash/db",
    "db:migrate": "pnpm run migrate -w @web3cash/db",
    "db:migrate:deploy": "pnpm run migrate:deploy -w @web3cash/db",
    "db:studio": "pnpm run studio -w @web3cash/db",
    "db:seed": "pnpm run seed -w @web3cash/db",
    "infra:up": "docker compose -f infra/docker-compose.yml up -d",
    "infra:down": "docker compose -f infra/docker-compose.yml down",
    "infra:logs": "docker compose -f infra/docker-compose.yml logs -f",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\""
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "prettier": "^3.3.3",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
