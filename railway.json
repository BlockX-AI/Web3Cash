{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && cd packages/db && pnpm run generate && cd ../../apps/api && pnpm run build && cd ../worker && pnpm run build"
  },
  "deploy": {
    "startCommand": "cd packages/db && pnpm run migrate:deploy:prod && cd ../../apps/api && pnpm run start:prod",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1,
    "runtime": "V2"
  }
}
